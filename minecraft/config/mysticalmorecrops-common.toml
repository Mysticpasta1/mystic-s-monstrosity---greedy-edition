
#General configuration options.
[General]
	#Should the crop " supremium " be enabled?
	spuremiumCropEnabled = true
	#Should the crop " prosperity " be enabled?
	prosperityCropEnabled = true
	#Should the crop "anti matter" be enabled? (If there is Not "Mekanism" mod, this crop is disabled.)
	antiMatterCropEnabled = true
	#Should the crop "enriched thermonuclear" be enabled? (If there is Not "Mekanism Extras" mod, this crop is disabled.)
	enrichedThermonuclearCropEnabled = true

